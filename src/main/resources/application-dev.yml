spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
     ddl-auto: create-drop # 테스트 환경에서 테이블 생성 후 종료 시 삭제
    show-sql: true # Hibernate가 실행하는 SQL 로그 표시